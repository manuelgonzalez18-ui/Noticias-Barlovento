/*
 * Scripts propios de Noticias Barlovento.
 *
 * Desactivado por defecto: el encolado esta comentado en includes/assets.php.
 * Al activarlo, recordar subir NB_CORE_VERSION para romper la cache.
 */
( function () {
	'use strict';

	document.addEventListener( 'DOMContentLoaded', function () {
		// Aqui van los scripts del sitio.
	} );
}() );
